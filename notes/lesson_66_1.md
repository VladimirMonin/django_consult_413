# Lesson 66.1 Сессионная модель в Django и Пользователи 📚

## Что такое сессия? 🔍

Сессия — это механизм, который позволяет серверу отслеживать взаимодействие с клиентом (обычно браузером) на протяжении определенного времени. В контексте веб-приложений сессия используется для хранения информации о пользователе между запросами. Это может быть как анонимный пользователь, так и обычный.

Сессии играют ключевую роль в веб-разработке, так как HTTP-протокол является stateless, то есть каждый запрос к серверу обрабатывается независимо от других. Без сессий сервер не смог бы различать запросы от одного и того же пользователя, что сделало бы невозможным реализацию таких функций, как аутентификация, персонализация контента или сохранение состояния корзины покупок.

> [!info]
>
> #### Основные характеристики сессий 📌
>
> - Сессии позволяют сохранять состояние между HTTP-запросами, которые по своей природе являются stateless.
> - Они используются для хранения данных о пользователе, таких как аутентификация, предпочтения, корзина покупок и другие пользовательские данные.
> - Сессии могут быть реализованы на стороне сервера или клиента, но в Django они обычно управляются на стороне сервера.

Сессии в Django реализованы с использованием middleware, что делает их использование прозрачным для разработчика. Когда пользователь впервые посещает сайт, Django автоматически создает новую сессию и присваивает ей уникальный идентификатор, который сохраняется в куки браузера пользователя.

## Как Django хранит сессии? 🗃️

В Django сессии хранятся в базе данных в модели `django_session`. Содержимое сессионной куки `sessionid` шифруется и подписывается с использованием секретного ключа Django (`SECRET_KEY`). Это также хранится в БД в модели `django_session` в поле `session_data`.

Django предоставляет несколько вариантов хранения сессий, которые можно настроить в настройках проекта. По умолчанию используется базовый бэкенд, который хранит сессии в базе данных. Однако можно выбрать и другие варианты, такие как кэширование или файловое хранилище.

> [!info]
>
> #### Ленивая сессия 🐢
>
> Пока ничего не меняется, мы не делаем запись в базу данных. Это означает, что изменения в сессии сохраняются только при необходимости, что уменьшает нагрузку на базу данных.

Ленивое сохранение сессий — это важная оптимизация, которая уменьшает количество записей в базу данных. Сессия обновляется в базе данных только тогда, когда в ней действительно происходят изменения. Это особенно полезно для сайтов с высокой нагрузкой, где сессии используются интенсивно.

> [!warning]
>
> #### Пример работы с сессией в Django 🛠️
>
> В Django сессии можно использовать для хранения данных в виде словаря. Например, в представлении можно сохранить данные в сессии следующим образом:
>
> ```python
> def my_view(request):
>     request.session['my_key'] = 'my_value'
>     return HttpResponse("Session data saved")
> ```

Работа с сессиями в Django очень проста благодаря удобному API. Вы можете сохранять любые сериализуемые данные в сессии, просто добавляя их в словарь `request.session`. Django автоматически обрабатывает сериализацию и десериализацию данных.

## Middleware и как это связано с сессиями 🧩

Middleware в Django представляет собой "луковую" систему, где каждый слой выполняет определенные функции. Два middleware, `SessionMiddleware` и `AuthenticationMiddleware`, обрабатывают данные сессий и аутентификации соответственно.

Middleware в Django — это мощный механизм, который позволяет обрабатывать запросы и ответы на разных этапах их жизненного цикла. Сессионное middleware добавляет объект сессии в каждый входящий запрос, что позволяет представлениям легко работать с сессиями.

> [!info]
>
> #### SessionMiddleware 🧅
>
> - Обрабатывает сессии, обеспечивая их создание, чтение и запись.
> - Устанавливает сессионные куки и управляет данными сессий.

SessionMiddleware отвечает за создание, чтение и обновление сессий. Оно добавляет атрибут `session` к объекту запроса, который затем может быть использован в представлениях для доступа к данным сессии.

> [!info]
>
> #### AuthenticationMiddleware 🔐
>
> - Использует данные сессий для аутентификации пользователей.
> - Связывает пользователей с их сессиями, обеспечивая безопасность и персонализацию.

AuthenticationMiddleware работает в tandem с SessionMiddleware. Оно использует данные сессии для аутентификации пользователей, связывая объект пользователя с текущей сессией. Это позволяет легко получать доступ к информации о текущем пользователе в любом представлении.

## Пример с корзиной интернет-магазина 🛒

В интернет-магазине сессии могут использоваться для хранения данных корзины покупок. Например, можно сохранить идентификаторы товаров, добавленных в корзину, в сессии пользователя.

Сессии особенно полезны для реализации корзины покупок, так как они позволяют сохранять состояние корзины между запросами. Это означает, что пользователь может добавлять товары в корзину, перемещаться по сайту и даже закрывать браузер, а при возврате на сайт его корзина останется в том же состоянии.

> [!info]
>
> #### Модель корзины 📦
>
> Модель корзины может быть реализована как часть сессии, где данные хранятся в виде словаря.

Корзина покупок в сессии обычно реализуется как словарь, где ключами являются идентификаторы товаров, а значениями — количество или дополнительная информация о товаре. Такой подход позволяет легко добавлять, удалять и обновлять товары в корзине.

> [!info]
>
> #### Запись в сессии анонима, который туда что-то кладет 🛍️
>
> Для анонимных пользователей данные корзины могут храниться в сессии до тех пор, пока пользователь не авторизуется или не завершит покупку.

Для анонимных пользователей сессии являются единственным способом сохранения состояния корзины. Как только пользователь авторизуется, данные из сессии могут быть перенесены в базу данных и связаны с его учетной записью.

```python
def add_to_cart(request, product_id):
    if 'cart' not in request.session:
        request.session['cart'] = []
    request.session['cart'].append(product_id)
    request.session.modified = True
    return HttpResponse("Product added to cart")
```

Этот пример показывает, как можно добавить товар в корзину, хранящуюся в сессии. Обратите внимание на установку флага `modified` в `True`, что указывает Django на необходимость сохранения изменений в сессии.

> [!info]
>
> #### Пример использования сессий в Django 🛠️
>
> В Django сессии можно использовать для ограничения накрутки просмотров. Например, в представлении можно ограничить количество просмотров страницы заказа:

Сессии также полезны для реализации различных механизмов защиты от накрутки статистики. Например, можно ограничить количество просмотров страницы заказа, чтобы предотвратить искусственное завышение популярности товаров.

```python
class OrderDetailView(DetailView):
    model = Order
    template_name = "order_detail.html"
    context_object_name = "order"
    pk_url_kwarg = "order_id"

    def get_object(self, queryset=None):
        order = super().get_object(queryset)
        session_key = f"order_{order.id}_viewed"
        if not self.request.session.get(session_key):
            self.request.session[session_key] = True
            order.view_count = F("view_count") + 1
            order.save(update_fields=["view_count"])
            order.refresh_from_db()
        return order
```

В этом примере используется уникальный ключ сессии для каждого заказа, чтобы отслеживать, просматривал ли текущий пользователь этот заказ ранее. Если нет, то счетчик просмотров увеличивается. Это предотвращает многократное увеличение счетчика при повторных просмотрах одной и той же страницы заказа.

## Безопасность сессий 🔒

Безопасность сессий является критически важным аспектом веб-разработки. В Django реализованы несколько механизмов для защиты сессий от различных типов атак.

> [!warning]
>
> #### Основные меры безопасности сессий в Django
>
> - Использование `SECRET_KEY` для подписи сессионных данных
> - Возможность настройки времени жизни сессии
> - Защита от CSRF атак
> - Возможность использования HTTPS для передачи сессионных куки

Важно помнить, что сессионные куки должны передаваться только по защищенному соединению HTTPS, чтобы предотвратить их перехват. Также рекомендуется регулярно обновлять `SECRET_KEY` и следить за его безопасным хранением.

## Настройка сессий в Django ⚙️

Django предоставляет множество настроек для тонкой настройки поведения сессий. Эти настройки можно найти в файле настроек проекта.

> [!info]
>
> #### Основные настройки сессий
>
> - `SESSION_COOKIE_AGE` — время жизни сессионного куки в секундах
> - `SESSION_COOKIE_NAME` — имя сессионного куки
> - `SESSION_ENGINE` — бэкенд для хранения сессий
> - `SESSION_SAVE_EVERY_REQUEST` — сохранять ли сессию при каждом запросе

Настройка этих параметров позволяет адаптировать поведение сессий под конкретные нужды проекта. Например, можно увеличить время жизни сессии для сайтов, где важно сохранять состояние пользователя на длительный период.

## Настройка сессий в SETTINGS 🔧

В Django можно гибко настраивать параметры сессий через файл настроек проекта. Это позволяет адаптировать поведение сессий под конкретные требования вашего приложения.
Помимо базовых настроек, Django позволяет управлять и другими аспектами сессий. Например, можно указать, следует ли использовать защищенные куки (`SESSION_COOKIE_SECURE`), которые будут отправляться только по HTTPS, или ограничить доступ к куки только по HTTP (`SESSION_COOKIE_HTTPONLY`), чтобы предотвратить доступ к ним из JavaScript. Эти меры значительно повышают безопасность вашего приложения.

> [!info]
>
> #### Настройка времени жизни сессии
>
> В Django можно установить короткое время жизни сессии, например, 3 дня, но с возможностью автоматического продления при активности пользователя. Это реализуется через настройку `SESSION_COOKIE_AGE` и использование механизма обновления сессии при каждом запросе.

Для реализации сессии с автоматическим продлением при активности пользователя, можно использовать следующий подход:

```python
# В settings.py
SESSION_COOKIE_AGE = 3 * 24 * 60 * 60  # 3 дня в секундах
SESSION_SAVE_EVERY_REQUEST = True  # Обновлять сессию при каждом запросе
```

Важно понимать, что `SESSION_SAVE_EVERY_REQUEST = True` может создавать дополнительную нагрузку на базу данных, так как сессия будет перезаписываться при каждом запросе, даже если данные в ней не изменились. Поэтому эту настройку следует использовать с осторожностью, взвешивая преимущества постоянной активности сессии и потенциальные издержки производительности.
Такая конфигурация установит время жизни сессии в 3 дня, но при каждом запросе от пользователя сессия будет автоматически продлеваться. Это полезно для поддержания активных сессий пользователей, которые регулярно посещают сайт, при этом обеспечивая автоматическое завершение сессий для неактивных пользователей.

## Работа с сессиями в шаблонах 🎨

Для того чтобы `request.session` был доступен в шаблонах, необходимо, чтобы в `TEMPLATES` в `settings.py` был подключен контекстный процессор `'django.template.context_processors.request'`. Обычно он включен по умолчанию в новых проектах Django. Это позволяет передавать объект запроса в контекст шаблона, открывая доступ к данным сессии и другой информации, связанной с запросом.
Django предоставляет удобные инструменты для работы с сессиями не только в представлениях, но и в шаблонах. Это позволяет легко отображать данные сессии на веб-страницах.

> [!info]
>
> #### Пример использования сессий в шаблоне
>
> В шаблоне можно получить доступ к данным сессии через переменную `{{ request.session }}`. Например, чтобы отобразить количество товаров в корзине:
>
> ```html
> <div class="cart-info">
>     Товаров в корзине: {{ request.session.cart|length }}
> </div>
> ```

Работа с сессиями в шаблонах делает их использование еще более удобным. Вы можете легко отображать информацию из сессии, такую как количество товаров в корзине или имя авторизованного пользователя, прямо на веб-страницах.

## Заключение 🎯

Сессии в Django предоставляют мощный и удобный механизм для сохранения состояния между запросами. Они являются фундаментальной частью веб-разработки, позволяя реализовывать такие важные функции, как аутентификация пользователей, персонализация контента и сохранение состояния корзины покупок.

Понимание работы сессий и умение эффективно их использовать — важный навык для любого Django-разработчика. Правильное использование сессий может значительно улучшить пользовательский опыт и сделать ваше веб-приложение более функциональным и удобным.

В следующих уроках мы более подробно рассмотрим работу с пользователями в Django, включая аутентификацию, авторизацию и управление профилями пользователей. Эти темы тесно связаны с сессиями и дополняют их функциональность.
