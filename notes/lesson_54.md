# Создание моделей для приложения 🏗️

В этом разделе мы погрузимся в одну из ключевых концепций Django — ORM (Object-Relational Mapping) и создадим модели для нашего приложения. Модели — это "единственный и окончательный источник данных" о вашей информации. Они определяют структуру данных, их типы, связи между ними и правила валидации. Каждая модель в Django — это класс Python, который наследуется от `django.db.models.Model` и представляет собой таблицу в базе данных.

## Модель Мастера (`Masetr`) 🧑‍🔧

Начнем с создания модели для мастера. Эта модель будет хранить информацию о специалистах, которые предоставляют услуги в нашем салоне. На данном этапе нам важно хранить имя мастера и его контактный телефон. Эта информация будет использоваться для отображения в расписании и для связи с клиентами.

### Что такое модель в Django?

Модель в Django — это не просто описание таблицы в базе данных. Это мощный инструмент, который инкапсулирует всю логику работы с данными. Вместо того чтобы писать SQL-запросы вручную, мы будем использовать методы и атрибуты Python-классов. Django ORM автоматически преобразует наши операции с объектами в SQL-код. Такой подход ускоряет разработку, уменьшает количество ошибок и делает код более читаемым и поддерживаемым. Представьте, что вам больше не нужно беспокоиться о синтаксисе SQL для разных СУБД (PostgreSQL, MySQL, SQLite) — Django берет эту работу на себя. Вы просто работаете с Python-объектами, а фреймворк обеспечивает их сохранение, извлечение и обновление в базе данных.

Опишем модель `Masetr` в файле `core/models.py`. Обратите внимание, что название модели содержит опечатку ("Masetr" вместо "Master"), но мы будем использовать её как есть, чтобы соответствовать исходному коду проекта.

```python
# core/models.py
from django.db import models

class Masetr(models.Model):
    name = models.CharField(max_length=100)
    phone = models.CharField(max_length=20)
```

### Определение полей модели

Каждый атрибут класса модели, который является экземпляром класса поля (например, `models.CharField`), представляет собой колонку в таблице базы данных. Django использует эти определения для создания SQL-схемы (команда `CREATE TABLE`). Тип поля (`CharField`, `IntegerField`, `DateTimeField` и т.д.) сообщает Django, какой тип данных использовать в базе данных, а также какой виджет использовать в формах и админ-панели для отображения этого поля. Кроме того, поля могут иметь различные аргументы для настройки их поведения, например, для валидации данных.

>[!warning]
>
>#### Обязательный параметр `max_length`
>
>Для полей типа `CharField` **всегда** необходимо указывать атрибут `max_length`. Он определяет максимальную длину строки, которую можно сохранить в этом поле на уровне базы данных. В исходном коде проекта этот параметр отсутствовал, что привело бы к ошибке при создании миграций. Мы исправили это, добавив `max_length=100` для имени и `max_length=20` для телефона. Это не только требование Django, но и хорошая практика проектирования баз данных, так как она помогает оптимизировать хранение и производительность.

## Модель Заказа (`Order`) 📝

Теперь создадим модель для хранения информации о записях клиентов к мастерам. Модель `Order` будет содержать данные о клиенте (имя и телефон), комментарий к заказу и, что самое важное, — связь с конкретным мастером. Эта модель станет центральной в логике нашего приложения.

### Связывание моделей: `ForeignKey`

Модель `Order` связана с моделью `Masetr` отношением "многие-к-одному". Это означает, что у одного мастера может быть много заказов, но каждый заказ может принадлежать только одному мастеру. Для реализации такой связи в Django используется поле `models.ForeignKey`. Это поле создает в таблице `Order` колонку (по умолчанию с именем `master_id`), которая будет хранить первичный ключ (ID) связанной записи из таблицы `Masetr`. Таким образом, база данных сможет эффективно связывать записи из двух таблиц.

Помимо `ForeignKey`, Django предлагает и другие типы связей:
- **`ManyToManyField`**: для отношений "многие-ко-многим" (например, один пост в блоге может иметь много тегов, и один тег может быть у многих постов).
- **`OneToOneField`**: для отношений "один-к-одному" (например, профиль пользователя, который расширяет стандартную модель `User`).

В нашем случае `ForeignKey` — идеальный выбор, так как заказ не может быть выполнен несколькими мастерами одновременно, а мастер может выполнить множество заказов.

```python
# core/models.py
# ... (класс Masetr)

class Order(models.Model):
    name = models.CharField(max_length=100)
    phone = models.CharField(max_length=20)
    comment = models.CharField(max_length=255)
    master = models.ForeignKey(
        "Masetr",
        on_delete=models.SET_NULL,
        null=True,
        related_name='orders' # Добавим related_name
    )
```

Мы передаем в `ForeignKey` первым аргументом модель, с которой устанавливается связь. В данном случае это `"Masetr"`. Использование строки вместо прямого указания класса `Masetr` является хорошей практикой, чтобы избежать проблем с циклическими импортами.

>[!info]
>#### Обратный доступ к объектам и `related_name`
>Когда вы определяете `ForeignKey` от `Order` к `Masetr`, каждый объект `Order` получает доступ к своему мастеру через атрибут `order.master`. Но как получить все заказы для конкретного мастера? По умолчанию Django создает для этого атрибут `master.order_set`. Однако имя `order_set` не всегда интуитивно. С помощью параметра `related_name='orders'` мы можем переопределить это имя. Теперь для получения всех заказов мастера мы можем использовать более понятный синтаксис: `master.orders.all()`.

### Поведение при удалении: `on_delete`

Атрибут `on_delete` в `ForeignKey` — это критически важный параметр, который определяет, что произойдет со связанными объектами (в нашем случае, с заказами), если объект, на который они ссылаются (мастер), будет удален. Django требует явного указания этого параметра, чтобы разработчик осознанно принимал решение о целостности данных. Это защищает от случайной потери данных и заставляет задуматься об архитектуре.

Рассмотрим основные варианты `on_delete`:
- **`models.CASCADE`**: Самый распространенный вариант. При удалении мастера будут удалены все связанные с ним заказы. Используется, когда дочерние объекты не могут существовать без родительского (например, комментарии к посту).
- **`models.PROTECT`**: Запрещает удаление мастера, если у него есть хотя бы один связанный заказ. При попытке удаления будет вызвано исключение `ProtectedError`. Это полезно, когда нужно обеспечить максимальную сохранность связанных данных.
- **`models.SET_NULL`**: Наш выбор. Если мастер удален, поле `master` в его заказах устанавливается в `NULL`. Это позволяет сохранить историю заказов, но "отвязать" их от удаленного мастера. Требует, чтобы поле в базе данных могло быть пустым (`null=True`).
- **`models.SET_DEFAULT`**: Устанавливает для поля `master` значение по умолчанию, которое должно быть определено с помощью параметра `default`. Например, можно создать "гостевого" или "архивного" мастера и привязывать к нему заказы удаленных специалистов.
- **`models.DO_NOTHING`**: Ничего не делает. Это перекладывает ответственность за целостность данных на уровень базы данных. Если в БД нет соответствующих ограничений (constraints), это может привести к "битым" ссылкам и ошибкам. Использовать следует с большой осторожностью.

Выбор правильной стратегии `on_delete` зависит исключительно от бизнес-логики вашего приложения.

>[!info]
>
>#### Особенности `on_delete`
>
>Мы использовали значение `models.SET_NULL`. Это означает, что если мастер будет удален из базы данных, поле `master` во всех связанных с ним заказах установится в `NULL`. Это позволяет сохранить историю заказов, даже если мастер больше не работает. Для использования `SET_NULL` необходимо также разрешить полю быть пустым на уровне базы данных, добавив атрибут `null=True`.

Существуют и другие опции, например `models.CASCADE`, при которой удаление мастера привело бы к удалению всех его заказов. Выбор зависит от бизнес-логики вашего приложения. `CASCADE` используется, когда дочерние записи не могут существовать без родительской (например, комментарии к посту в блоге). `SET_NULL` подходит, когда дочерние записи имеют ценность сами по себе.

## Миграции ⚙️

После того как мы определили наши модели, нам нужно сообщить базе данных об этих изменениях. Простое создание Python-классов никак не влияет на структуру таблиц в БД. Этот процесс синхронизации моделей и базы данных в Django управляется системой миграций. Миграции — это, по сути, "система контроля версий для схемы вашей базы данных".

### Создание миграций

Миграции — это файлы с инструкциями на Python, которые описывают, как изменить схему базы данных, чтобы она соответствовала вашим моделям. Чтобы сгенерировать эти файлы, используется команда `makemigrations`. Django сравнивает текущее состояние ваших моделей с состоянием, зафиксированным в последнем файле миграций, и автоматически генерирует код для устранения различий.

```bash
poetry run python manage.py makemigrations
```

Эта команда не выполняет никаких изменений в базе данных. Она лишь создает файл (например, `core/migrations/0001_initial.py`), который содержит описание операций: `CreateModel`, `AddField`, `RemoveField` и т.д. Эти файлы следует рассматривать как часть исходного кода вашего проекта и обязательно добавлять в систему контроля версий (Git). Это гарантирует, что все разработчики в команде и все окружения (dev, staging, production) будут иметь одинаковую и актуальную схему БД.

### Применение миграций

Сгенерированные миграции нужно "применить" к базе данных. Это делает команда `migrate`. Она просматривает специальную таблицу `django_migrations` в базе данных, чтобы определить, какие миграции уже были выполнены, а затем последовательно выполняет все новые. Этот процесс является надежным и транзакционным — если какая-то миграция завершится с ошибкой, вся транзакция будет отменена, и база данных останется в исходном состоянии.

```bash
poetry run python manage.py migrate
```

При первом запуске `migrate` создаст не только таблицы для ваших моделей, но и таблицы для встроенных приложений Django: `auth` (аутентификация), `admin` (админ-панель), `sessions` (сессии) и др.

### Проверка статуса миграций

Иногда полезно проверить, какие миграции уже применены, а какие — нет, особенно при работе в команде или при переключении между ветками в Git. Для этого существует команда `showmigrations`. Она предоставляет наглядное представление о состоянии миграций для каждого приложения в проекте.

```bash
poetry run python manage.py showmigrations
```

Вы увидите список всех миграций во всех приложениях проекта. Примененные миграции будут отмечены крестиком `[X]`. Это помогает диагностировать проблемы, когда база данных не соответствует ожиданиям, и понять, какие изменения еще не были применены.

>[!highlight]
>
>#### Всегда выполняйте миграции
>
>После **любых** изменений в файле `models.py` (добавление/удаление/изменение полей или моделей) необходимо повторить цикл из двух команд: `makemigrations` и `migrate`. Пропуск этого шага приведет к рассинхронизации между кодом и базой данных, что неизбежно вызовет ошибки в работе приложения.

## Создание суперпользователя 🦸

Для удобного управления данными в приложении Django предоставляет встроенную административную панель. Это мощный инструмент для внутреннего использования, который позволяет доверенным пользователям управлять контентом сайта. Чтобы получить к ней доступ, нам нужен пользователь с максимальными правами — суперпользователь.

### Команда `createsuperuser`

Суперпользователь в Django — это специальный аккаунт, у которого есть все возможные разрешения (`is_staff=True`, `is_superuser=True` и все назначенные права). Создать такого пользователя можно с помощью специальной интерактивной команды.

```bash
poetry run python manage.py createsuperuser
```

Вам будет предложено ввести имя пользователя (логин), адрес электронной почты и пароль (дважды для подтверждения). После успешного создания вы можете использовать эти учетные данные для входа в админку. Эта команда является частью встроенного приложения `django.contrib.auth`, которое управляет всей системой аутентификации и авторизации в Django.

## Регистрация моделей в админке 🖥️

По умолчанию административная панель пуста, даже после создания моделей и суперпользователя. Django придерживается принципа явного указания, поэтому мы должны сами решить, какие из наших моделей должны быть доступны для управления через админку. Это защищает от случайного раскрытия служебных моделей.

### Файл `admin.py` и регистрация

Регистрация моделей происходит в файле `admin.py` соответствующего приложения. Для простой регистрации достаточно импортировать модель и вызвать `admin.site.register`.

Это самый простой способ. Однако мощь админки Django раскрывается через классы `ModelAdmin`, которые позволяют тонко настраивать внешний вид и поведение интерфейса для каждой модели: какие поля отображать в списке, по каким полям можно фильтровать и искать, как группировать поля в форме редактирования и многое другое.

Добавьте следующий код в `core/admin.py`:

```python
# core/admin.py
from django.contrib import admin
from .models import Masetr, Order

admin.site.register(Masetr)
admin.site.register(Order)
```

Теперь, если вы запустите сервер разработки (`poetry run python manage.py runserver`) и перейдете по адресу `http://127.0.0.1:8000/admin/`, вы сможете войти под учетной записью суперпользователя и увидите разделы для управления мастерами и заказами. Это позволяет добавлять, редактировать и удалять записи через удобный веб-интерфейс без необходимости писать SQL-запросы. В дальнейшем мы научимся настраивать отображение моделей в админ-панели, чтобы сделать ее еще более удобной.
