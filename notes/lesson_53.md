# Тема Django. Наследование шаблонов. Include. Контекстный процессор. Урок 53

## Что такое наследование шаблонов в Django? 🤔

Наследование шаблонов в Django — это мощный механизм, который позволяет создавать базовые шаблоны с общими элементами и затем расширять их в дочерних шаблонах. Это значительно упрощает разработку и поддержку веб-приложений, поскольку позволяет избежать дублирования кода и обеспечивает единообразие дизайна на всем сайте. Вместо того чтобы повторять одни и те же HTML-структуры, такие как шапка, подвал или навигационное меню, в каждом файле, мы можем определить их один раз в базовом шаблоне.

Наследование шаблонов является ключевым аспектом эффективной разработки на Django, позволяя разработчикам создавать сложные и масштабируемые веб-приложения с минимальным дублированием кода. Этот подход способствует чистоте и поддерживаемости кодовой базы, что особенно важно в больших проектах.

### Что такое наследование шаблонов? 💡

Наследование шаблонов в Django — это мощный механизм, который позволяет создавать базовые шаблоны с общими элементами и затем расширять их в дочерних шаблонах. Это значительно упрощает разработку и поддержку веб-приложений, поскольку позволяет избежать дублирования кода и обеспечивает единообразие дизайна на всем сайте. Вместо того чтобы повторять одни и те же HTML-структуры, такие как шапка, подвал или навигационное меню, в каждом файле, мы можем определить их один раз в базовом шаблоне.

Наследование шаблонов является ключевым аспектом эффективной разработки на Django, позволяя разработчикам создавать сложные и масштабируемые веб-приложения с минимальным дублированием кода. Этот подход способствует чистоте и поддерживаемости кодовой базы, что особенно важно в больших проектах.

### Теги `{% block %}` и `{% include %}` 🧱

В основе наследования шаблонов лежат два ключевых тега: `{% block %}` и `{% include %}`. Тег `{% block %}` определяет области в базовом шаблоне, которые могут быть переопределены дочерними шаблонами. Это позволяет дочерним шаблонам вставлять свой уникальный контент в эти предопределенные "слоты". Например, можно создать блок для заголовка страницы (`title`) или для основного содержимого (`content`).

Тег `{% include %}`, в свою очередь, используется для вставки содержимого другого шаблона в текущий. В отличие от `{% extends %}`, `{% include %}` не создает иерархии наследования, а просто вставляет содержимое файла. Это полезно для повторного использования небольших, независимых фрагментов HTML, таких как навигационные меню, футеры или виджеты. Например, если у вас есть повторяющийся элемент, такой как карточка товара, вы можете вынести его в отдельный файл и включать его в любом месте, где это необходимо.

### Тег `{% extends %}` 🔗

Тег `{% extends %}` является краеугольным камнем системы наследования шаблонов Django. Он всегда должен быть первой строкой в дочернем шаблоне и указывает, какой базовый шаблон этот дочерний шаблон будет расширять. Когда Django обрабатывает шаблон, содержащий `{% extends %}`, он сначала загружает указанный базовый шаблон, а затем замещает или дополняет его содержимое в соответствии с блоками, определенными в дочернем шаблоне.

Использование `{% extends %}` позволяет создавать сложную иерархию шаблонов, где один базовый шаблон может быть расширен несколькими дочерними, а те, в свою очередь, могут служить базой для других шаблонов. Это обеспечивает гибкость и модульность в структуре вашего фронтенда.

### Переопределение, расширение и значения по умолчанию для блоков 🔄

Блоки, определенные с помощью `{% block %}`, предоставляют гибкие возможности для управления контентом. Дочерний шаблон может полностью **переопределить** содержимое блока, просто объявив блок с тем же именем и поместив в него новый контент. Это означает, что все, что было в родительском блоке, будет заменено.

Однако иногда требуется не полностью заменить, а **расширить** содержимое родительского блока. Для этого внутри дочернего блока используется переменная `{{ block.super }}`. Она вставляет содержимое родительского блока в текущий, позволяя добавить дополнительный контент до или после него. Это особенно полезно, когда нужно сохранить часть базового функционала, но при этом добавить специфические элементы.

Кроме того, блокам можно задавать **значения по умолчанию** в базовом шаблоне. Если дочерний шаблон не переопределяет определенный блок, то будет использовано содержимое, указанное в базовом шаблоне. Это обеспечивает запасной вариант и гарантирует, что даже без явного переопределения шаблон будет отображаться корректно.

Изображение `templates_extends_schema.png` представляет собой схему, иллюстрирующую концепцию наследования шаблонов. На ней показан базовый шаблон (`base.html`) с определенными блоками, такими как `header`, `content` и `footer`. От этого базового шаблона отходят стрелки к дочерним шаблонам (`page1.html`, `page2.html`), которые расширяют `base.html` и переопределяют или дополняют его блоки. Это наглядно демонстрирует, как дочерние шаблоны используют структуру родительского, вставляя свой уникальный контент в предопределенные области.

![templates_extends_schema.png](./images/templates_extends_schema.png)

## Простой пример наследования шаблонов в Django 📝

Рассмотрим базовый шаблон `base.html`, который определяет общую структуру страницы:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        {% block title %}
        "Барбершоп "Арбуз"
        {% endblock title %}
    </title>
</head>
<body>
    {% block content %}{% endblock content %}
</body>
</html>
```

Дочерний шаблон `thanks.html` расширяет базовый и переопределяет блок `content`:

```html
{% extends "base.html" %}
{% block content %}
    <h1>Спасибо за заявку!</h1>
    <p>Мы свяжемся с вами в ближайшее время!</p>
{% endblock content %}
```

>[!info]
>
>#### Как это работает? ⚙️
>
>1. Дочерний шаблон указывает родительский через `{% extends %}`
>2. Блок `content` переопределяется в дочернем шаблоне
>3. Все остальные элементы (например, `title`) остаются из базового шаблона

Результат можно увидеть на скриншоте:
![first_template_exends.png](./images/first_template_exends.png)

## Простой пример `include` в Django 📝

Тег шаблона `include` позволяет включать содержимое одного шаблона в другой. В отличие от `extends`, он не создает иерархии наследования, а просто вставляет содержимое файла.

### Как использовать `include`? 🧩

Рассмотрим пример навигационного меню, вынесенного в отдельный файл `include_nav_menu.html`:

```html
<nav>
    <ul>
        <li><a href="">Текст ссылки</a></li>
        <li><a href="">Текст ссылки</a></li>
        <li><a href="">Текст ссылки</a></li>
        <li><a href="">Текст ссылки</a></li>
        <li><a href="">Текст ссылки</a></li>
    </ul>
</nav>
```

Теперь мы можем включить это меню в базовый шаблон `base.html`:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        {% block title %}
        "Барбершоп "Арбуз"
        {% endblock title %}
    </title>
</head>
<body>
    <header>
        {% block header %}
        {% include "include_nav_menu.html" %}
        {% endblock header %}
    </header>
    <main>
        {% block content %}{% endblock content %}
    </main>
    <footer>
    </footer>
</body>
</html>
```

>[!info]
>
>#### Преимущества `include` 🚀
>
>- **Повторное использование**: Один компонент можно включать в разные места
>- **Модульность**: Каждый компонент живет в отдельном файле
>- **Гибкость**: Можно включать в блоки для управления видимостью
>- **Удобство поддержки**: Изменения вносятся в одном месте

Размещение `include` внутри блока (`header`) позволяет при необходимости переопределить или скрыть меню в дочерних шаблонах.

### Как работает `{% include %}`? ⚙️

Тег `{% include %}` в Django шаблонах позволяет вставлять содержимое одного шаблона в другой. Это отличается от наследования (`{% extends %}`), так как `include` просто "включает" код, не создавая иерархических связей. Это особенно полезно для повторного использования небольших, автономных компонентов, таких как навигационные меню, футеры или виджеты. Например, в нашем `base.html` мы включаем `include_nav_menu.html` в блок `header`.

```html
    <header>
        {% block header %}
        {% include "include_nav_menu.html" %}
        {% endblock header %}
    </header>
```

Такой подход позволяет легко управлять общими элементами интерфейса, делая код более модульным и читаемым.

### Передача данных в `include` и управление отображением 📊

Ссылки меню и их текст могут быть динамически переданы в шаблон через контекст представления (`views.py`). Это означает, что вы можете формировать данные для меню в Python-коде и затем передавать их в шаблон `include_nav_menu.html`, который будет их отображать. Это обеспечивает гибкость и позволяет изменять содержимое меню без изменения самого HTML-шаблона.

```html
<nav>
    <ul>
        <li><a href="">Текст ссылки</a></li>
        <li><a href="">Текст ссылки</a></li>
        <li><a href="">Текст ссылки</a></li>
        <li><a href="">Текст ссылки</a></li>
        <li><a href="">Текст ссылки</a></li>
    </ul>
</nav>
```

Кроме того, если `include` находится внутри блока (`{% block %}`), как в нашем примере с `header`, вы можете полностью отключить отображение навигационного меню на определенной странице. Для этого достаточно переопределить этот блок в дочернем шаблоне, оставив его пустым или вставив другое содержимое.

```html
{% extends "base.html" %}
{% block header %}
    {# Здесь можно оставить блок пустым или добавить другой контент #}
{% endblock header %}
```

Это дает полный контроль над тем, какие элементы отображаются на каждой странице, сохраняя при этом общую структуру базового шаблона.

## Пример с `include` в цикле шаблонизатора 📝

Использование тега `{% include %}` внутри циклов шаблонизатора Django является **очень мощным инструментом** для создания динамически генерируемого контента. Этот подход позволяет вынести повторяющиеся элементы интерфейса, такие как карточки товаров, элементы списка или записи блога, в отдельные, переиспользуемые шаблоны. Таким образом, мы значительно сокращаем дублирование кода и делаем шаблоны более читаемыми и поддерживаемыми.

Рассмотрим пример, где мы отображаем список заявок. Вместо того чтобы писать HTML-код для каждой карточки заявки непосредственно в шаблоне `orders_list.html`, мы выносим структуру одной карточки в отдельный файл `include_order_card.html`.

Вот как выглядит основной шаблон `orders_list.html`:

```html
    <h1>Список заявок</h1>
    <div class="flex-container">
        {% comment %} Проверка на пустую коллекцию empty{% endcomment %}
        {% for order in orders %}
        {% include "include_order_card.html" %}
        {% endfor %}
    </div>
```

В этом коде мы видим, что внутри контейнера `flex-container` используется цикл `{% for order in orders %}`. На каждой итерации цикла, для каждого объекта `order` из коллекции `orders`, мы включаем шаблон `include_order_card.html` с помощью тега `{% include %}`.

Содержимое шаблона `include_order_card.html` выглядит следующим образом:

```html
        <div class="flex-card">
            <h2>ID заявки: {{ order.id }}</h2>
            <p>Имя: {{ order.client_name }}</p>

            <p>Дата заявки: {{ order.date }}</p>
            <p class= {% if order.status == "новая" %}
            "new"
            {% elif order.status == "подтвержденная" %}
            "confirmed"
            {% elif order.status == "отмененная" %}
            "rejected"
            {% elif order.status == "выполненная" %}
            "canceled"
            {% endif %}
            >Статус заявки: {{ order.status }}</p>
            <p>Количество услуг: {{ order.services|length }}</p>
            <div class="services">
                {% comment %} цикл для span услуг {% endcomment %}
                {% for service  in order.services  %}
                <span class="service">{{ service }}</span>
                {% endfor %}
            </div>
        </div>
```

Важно отметить, что внутри `include_order_card.html` мы можем напрямую обращаться к переменной `order`, которая передается из родительского контекста цикла. Это позволяет каждой включенной карточке отображать данные, специфичные для текущей заявки. Такой подход не только упрощает структуру шаблонов, но и повышает их гибкость, позволяя легко изменять внешний вид карточки в одном месте, и эти изменения будут применены ко всем отображаемым карточкам.

>[!info]
>
>#### Преимущества `include` в циклах 🚀
>
>Использование `{% include %}` в циклах позволяет:
>
>- **Повторное использование кода**: Один и тот же компонент (`include_order_card.html`) используется многократно.
>- **Чистота кода**: Основной шаблон (`orders_list.html`) остается лаконичным и легко читаемым.
>- **Удобство поддержки**: Изменения в дизайне или структуре карточки вносятся только в одном файле.
>- **Динамическое отображение**: Каждая карточка отображает уникальные данные из текущего элемента цикла.

Таким образом, мы эффективно вынесли повторяющийся элемент (`flex-card`) в отдельный шаблон и рендерим его циклом внутри `flex-container`, что является отличным примером применения принципов DRY в Django.

## Подключение статики в Django 📝

### Подключение в базовом шаблоне

В базовый шаблон мы можем подключить:

- Стили и скрипты Bootstrap 5
- Иконки Bootstrap Icons
- Наши собственные статические файлы (CSS/JS)

Пример подключения в `base.html`:

```html
{% load static %}
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <!-- Наши стили -->
    <link rel="stylesheet" href="{% static 'css/main.css' %}">
```

А так же скрипты

```html
...
 {% comment %} Подключаю скрипты BS5 {% endcomment %}
     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
     {% comment %} Подключаю свои скрипты {% endcomment %}
     <script src="{% static 'js/script.js' %}"></script>
</body>
```

Но для корректной работы статических файлов в Django понадобятся дополнительные настройки.

Тег шаблона `{% static %}` является ключевым элементом для работы со статическими файлами в Django. Он генерирует полный URL-путь к статическому файлу, используя настройки из `settings.py`. Это особенно важно при развертывании, так как пути к статике могут отличаться на разных серверах.

Пример использования:

```html
<link rel="stylesheet" href="{% static 'css/main.css' %}">
<script src="{% static 'js/main.js' %}"></script>
```

>[!info]
>
>#### Как работает тег `static`? ⚙️
>
>1. Django ищет файл в папках, указанных в `STATICFILES_DIRS`
>2. Генерирует полный URL на основе `STATIC_URL`
>3. В продакшене собирает все файлы в одну папку (`collectstatic`)

>[!warning]
>
>#### Важно помнить
>
>- Всегда добавляйте `{% load static %}` в начале шаблона
>- Для новых файлов может потребоваться перезагрузка сервера
>- В продакшене нужно выполнить `python manage.py collectstatic`

Для того чтобы Django мог раздавать статические файлы, необходимо выполнить ряд настроек в файлах `settings.py` и `urls.py`. Эти шаги критически важны для того, чтобы ваш веб-сервер знал, где искать статические ресурсы и как их предоставлять пользователям.

На изображении ![static_tag_2.png](./images/static_tag_2.png) представлен пример того, как выглядит подключение статики в базовом шаблоне, включая тег `{% load static %}` в самом начале файла. Это подтверждает, что все необходимые стили и скрипты будут доступны для использования на страницах, наследующих этот базовый шаблон.

### `Settings.py` и настройка статики ⚙️

Для того чтобы Django знал, где искать статические файлы, необходимо определить две ключевые настройки в файле `settings.py`. Эти настройки указывают базовый URL для статических файлов и директории, в которых Django должен их искать.

Первая настройка — это `STATIC_URL`. Она определяет базовый URL, по которому будут доступны статические файлы в вашем веб-приложении. Например, если вы установите `STATIC_URL = '/static/'`, то все статические файлы будут доступны по адресу `http://yourdomain.com/static/`. Это важно для корректной работы тега `{% static %}`, который будет использовать этот URL для генерации путей.

Вторая настройка — `STATICFILES_DIRS`. Это список директорий, в которых Django будет искать статические файлы. Обычно здесь указывается путь к папке `static` вашего проекта, где хранятся общие статические файлы, не привязанные к конкретным приложениям. Использование `BASE_DIR / 'static'` гарантирует, что путь будет корректным независимо от операционной системы.

```python
STATIC_URL = '/static/'
STATICFILES_DIRS = [BASE_DIR / 'static']
```

>[!highlight]
>
>#### Разница между `STATIC_URL` и `STATICFILES_DIRS` 📌
>
>`STATIC_URL` — это URL-префикс для статических файлов в браузере, а `STATICFILES_DIRS` — это список файловых путей на сервере, где Django будет искать статические файлы. Не путайте эти две настройки, так как они выполняют разные, но взаимосвязанные функции.

### `urls.py` и настройка статики 🗺️

После настройки `settings.py` необходимо добавить маршруты для статических файлов в главный файл `urls.py` вашего проекта. Это позволит Django обслуживать статические файлы в режиме разработки. В продакшене статику обычно раздают веб-серверы, такие как Nginx или Apache, но для локальной разработки это необходимо.

Для этого нужно импортировать функции `settings` и `static` из `django.conf` и `django.conf.urls.static` соответственно. Затем, в список `urlpatterns`, который содержит все маршруты вашего проекта, добавляется специальный маршрут для статических файлов.

```python
from django.conf import settings
from django.conf.urls.static import static
from django.urls import path # Убедитесь, что path импортирован

urlpatterns = [
    path('admin/', admin.site.urls),
    # ... другие маршруты вашего приложения
] + static(settings.STATIC_URL, document_root=settings.STATICFILES_DIRS[0])
```

В этом коде `static(settings.STATIC_URL, document_root=settings.STATICFILES_DIRS[0])` создает URL-паттерн, который сопоставляет запросы к `STATIC_URL` (например, `/static/`) с файлами, расположенными в директории, указанной в `STATICFILES_DIRS`. Это позволяет Django находить и отдавать браузеру ваши CSS, JS и изображения.

>[!info]
>
>#### Важное замечание для продакшена ⚠️
>
>Данный способ раздачи статических файлов через `urls.py` предназначен **только для режима разработки**. В продакшене Django не должен напрямую обслуживать статические файлы. Для этого используются специализированные веб-серверы, которые более эффективно справляются с этой задачей.

## Пример — как это будет выглядеть в итоге 🚀

После всех настроек и использования тегов `{% extends %}`, `{% include %}` и `{% static %}`, структура наших шаблонов становится гораздо более модульной и поддерживаемой. На изображении ![static_and_extends_templates.png](images/static_and_extends_templates.png) представлен общий вид страницы, где видно, как все эти элементы гармонично сочетаются, формируя единое целое. Это позволяет создавать сложные веб-приложения с чистым, легко поддерживаемым кодом, где каждый компонент выполняет свою специфическую роль.

Интеграция наследования шаблонов, включения компонентов и управления статическими файлами в Django приводит к созданию эффективной и масштабируемой архитектуры фронтенда. Этот подход позволяет разработчикам сосредоточиться на логике приложения, минимизируя дублирование HTML-кода и обеспечивая единообразие пользовательского интерфейса. Изображение ![static_and_extends_templates.png](images/static_and_extends_templates.png) наглядно демонстрирует результат такого комплексного подхода, показывая, как различные части шаблона собираются вместе для формирования полноценной веб-страницы.

`include_order_card.html`

```html
        <div class="flex-card">
            <h2>ID заявки: {{ order.id }}</h2>
            <p>Имя: {{ order.client_name }}</p>

            <p>Дата заявки: {{ order.date }}</p>
            <p class= {% if order.status == "новая" %}
            "new"
            {% elif order.status == "подтвержденная" %}
            "confirmed"
            {% elif order.status == "отмененная" %}
            "rejected"
            {% elif order.status == "выполненная" %}
            "canceled"
            {% endif %}
            >Статус заявки: {{ order.status }}</p>
            <p>Количество услуг: {{ order.services|length }}</p>
            <div class="services">
                {% comment %} цикл для span услуг {% endcomment %}
                {% for service  in order.services  %}
                <span class="service">{{ service }}</span>
                {% endfor %}
            </div>
        </div>
```

Этот шаблон `include_order_card.html` представляет собой отдельный, самодостаточный компонент — карточку заказа. Благодаря его вынесению в отдельный файл, мы можем легко подключить эти карточки в любое место сайта, где нам это необходимо, используя тег `{% include %}`. Это обеспечивает высокую степень переиспользуемости и гибкости в дизайне, позволяя быстро вносить изменения в отображение карточек без необходимости редактировать множество файлов.

`orders_list.html`

```html
{% extends "base.html" %}
{% block content %}
<h1>Список заявок</h1>
    <div class="flex-container">
        {% comment %} Проверка на пустую коллекцию empty{% endcomment %}
        {% for order in orders %}
        {% include "include_order_card.html" %}
        {% endfor %}
    </div>
{% endblock content %}
```

В шаблоне `orders_list.html` мы видим, как используется наследование от `base.html` (`{% extends "base.html" %}`) и как в блок `content` вставляется заголовок и контейнер со списком заявок. Сами карточки заявок (`include_order_card.html`) включаются в цикле. Это демонстрирует, что у нас нет дублирования кода: логика отображения одной карточки вынесена отдельно, а основной шаблон остается чистым и сфокусированным на общей структуре страницы, что значительно упрощает его чтение и поддержку.

`base.html`

```html
{% load static %}
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% comment %} Подключаю 2мя ссылками BS5 и BS5 иконки {% endcomment %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    {% comment %} Подключаю стили {% endcomment %}
    <link rel="stylesheet" href="{% static 'css/main.css' %}">
    <title>
        {% block title %}
        "Барбершоп "Арбуз"
        {% endblock title %}
    </title>
</head>
<body>
    <header>
        {% block header %}
        {% include "include_nav_menu.html" %}
        {% endblock header %}
    </header>
    <main>
        {% block content %}{% endblock content %}
    </main>
    <footer>
    </footer>
    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Наши скрипты -->
    <script src="{% static 'js/main.js' %}"></script>
</body>
</html>
```

Базовый шаблон `base.html` является основой для всех остальных шаблонов. В нем мы подключаем все необходимые статические файлы, такие как стили Bootstrap и наши собственные CSS/JS файлы, используя тег `{% static %}`. Также здесь определены основные блоки (`header`, `content`, `title`), которые могут быть переопределены в дочерних шаблонах. Это позволяет нам использовать `base.html` как единую точку входа для всех общих элементов сайта, обеспечивая консистентность и упрощая дальнейшую разработку. В нем также можно создать дополнительные блоки, например, для стилей и скриптов, чтобы дочерние шаблоны могли добавлять свои специфические ресурсы, используя `{{ block.super }}`.

## Super в шаблонах ✨

Механизм `super` в Django шаблонах предоставляет мощный способ расширения содержимого блоков, определенных в родительских шаблонах, вместо их полного переопределения. Это особенно полезно, когда вы хотите добавить дополнительный контент к существующему блоку, сохраняя при этом его исходное содержимое.

### Что такое `{{ block.super }}`? 💡

`{{ block.super }}` — это специальная переменная, доступная внутри тега `{% block %}` в дочерних шаблонах. Она позволяет получить содержимое одноименного блока из родительского шаблона. Таким образом, вы можете вставить родительский контент в любое место внутри дочернего блока, а затем добавить свой собственный контент до или после него. Это обеспечивает гибкость и позволяет избежать дублирования кода, когда требуется лишь небольшое изменение или дополнение к базовому содержимому.

Например, если в базовом шаблоне определен блок `title`, и вы хотите добавить к нему специфический префикс или суффикс в дочернем шаблоне, не переписывая весь заголовок, `{{ block.super }}` будет идеальным решением.

### Как использовать `{{ block.super }}`? 🛠️

Использование `{{ block.super }}` очень просто. Вам нужно определить блок с тем же именем, что и в родительском шаблоне, и внутри этого блока использовать `{{ block.super }}` там, где вы хотите вставить содержимое родительского блока.

Рассмотрим пример. Пусть у нас есть базовый шаблон `base.html` с блоком `title`:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Мой сайт{% endblock title %}</title>
</head>
<body>
    {% block content %}{% endblock content %}
</body>
</html>
```

Теперь, если мы хотим в дочернем шаблоне `child.html` добавить к заголовку страницы название конкретной страницы, мы можем сделать это так:

```html
{% extends "base.html" %}

{% block title %}
    Страница контактов - {{ block.super }}
{% endblock title %}

{% block content %}
    <h1>Свяжитесь с нами</h1>
    <p>Наши контакты...</p>
{% endblock content %}
```

В этом примере `{{ block.super }}` вставит "Мой сайт" из `base.html`, и итоговый заголовок страницы будет "Страница контактов - Мой сайт". Это демонстрирует, как можно легко расширять содержимое блоков, не теряя при этом базовый контент.

> [!highlight]
>
> #### Гибкость с `{{ block.super }}` 🚀
>
> Использование `{{ block.super }}` позволяет создавать очень гибкие иерархии шаблонов. Вы можете добавлять контент до, после или даже внутри содержимого родительского блока, что дает полный контроль над финальным видом страницы, сохраняя при этом принцип DRY (Don't Repeat Yourself).
>
> Это особенно полезно для добавления мета-тегов, скриптов или стилей, которые специфичны для дочерней страницы, но при этом должны дополнять общие элементы, определенные в базовом шаблоне.

```

## Шаблонный тег `url` 🔗

Шаблонный тег `{% url %}` в Django позволяет генерировать URL-адреса на основе имен маршрутов из `urls.py`, что делает шаблоны более гибкими и независимыми от конкретных путей. Это особенно полезно при рефакторинге, когда пути могут измениться, но имена маршрутов остаются прежними.

### Что такое шаблонный тег `url`? 💡

Тег `{% url %}` динамически генерирует абсолютные пути на основе имен маршрутов. Вместо жесткого кодирования путей в шаблонах:

```html
<!-- Плохо: жестко закодированный путь -->
<a href="/services/">Услуги</a>

<!-- Хорошо: использование тега url -->
<a href="{% url 'services' %}">Услуги</a>
```

Преимущества:

- Избегаем "сломанных" ссылок при изменении URL-структуры
- Делаем код более читаемым и поддерживаемым
- Упрощаем рефакторинг маршрутов

### Псевдонимы маршрутов и их использование 🏷️

В `urls.py` мы даем маршрутам имена с помощью параметра `name`:

```python
# barbershop/urls.py
from django.urls import path
from . import views

urlpatterns = [
    path('', views.home, name='home'),
    path('services/', views.services, name='services'),
    path('about/', views.about, name='about'),
    path('contacts/', views.contacts, name='contacts'),
]
```

Теперь в шаблонах можно ссылаться на эти маршруты по именам:

```html
<a href="{% url 'home' %}">Главная</a>
<a href="{% url 'services' %}">Услуги</a>
```

### Передача параметров в URL 📤

Для маршрутов с параметрами тег `url` принимает дополнительные аргументы. Рассмотрим пример маршрута с параметром:

```python
# urls.py
path('service/<int:service_id>/', views.service_detail, name='service_detail')
```

В шаблоне можно передавать параметры двумя способами:

1. Именованные аргументы:

```html
<a href="{% url 'service_detail' service_id=1 %}">Услуга #1</a>
```

2. Позиционные аргументы:

```html
<a href="{% url 'service_detail' 1 %}">Услуга #1</a>
```

Именованные аргументы предпочтительнее, так как делают код более читаемым и устойчивым к изменениям порядка параметров.

### Обновление меню навигации 🍱

Используем тег `url` для улучшения меню из `include_nav_menu.html`:

```html
<nav>
    <ul>
        <li><a href="{% url 'home' %}">Главная</a></li>
        <li><a href="{% url 'services' %}">Услуги</a></li>
        <li><a href="{% url 'about' %}">О нас</a></li>
        <li><a href="{% url 'contacts' %}">Контакты</a></li>
    </ul>
</nav>
```

>[!info]
>
>#### Преимущества тега `url` 🚀
>
>#### Почему это лучше жестких путей?
>
>- **Стабильность**: Ссылки не сломаются при изменении URL-структуры
>- **Читаемость**: Код становится более понятным благодаря именам маршрутов
>- **Гибкость**: Легко рефакторить маршруты без изменения шаблонов
>- **Поддержка параметров**: Удобная работа с параметризованными URL

Теперь меню будет корректно работать даже если мы изменим пути в `urls.py`, так как ссылки генерируются динамически на основе имен маршрутов.

## Что такое контекстный процессор? ⚙️

Контекстный процессор в Django — это функция, которая добавляет данные в контекст всех шаблонов автоматически, без необходимости передавать их в каждом представлении. Это мощный инструмент для глобальных данных, которые нужны на многих страницах, таких как меню навигации, информация о пользователе или настройки сайта.

### Определение контекстного процессора 💡

Контекстный процессор — это функция Python, которая принимает объект запроса (`request`) и возвращает словарь, который будет объединен с контекстом шаблона. Django автоматически применяет все зарегистрированные процессоры перед рендерингом любого шаблона.

```python
# Пример простого контекстного процессора
def menu_processor(request):
    return {
        'menu_items': [
            {'title': 'Главная', 'url': '/'},
            {'title': 'Услуги', 'url': '/services'},
            {'title': 'О нас', 'url': '/about'},
        ]
    }
```

### Встроенные контекстные процессоры 🧩

Django включает несколько встроенных контекстных процессоров, которые добавляют часто используемые данные:

- `django.template.context_processors.request` - добавляет объект `request` в контекст
- `django.template.context_processors.auth` - добавляет информацию об аутентификации
- `django.template.context_processors.debug` - добавляет флаги отладки
- `django.template.context_processors.i18n` - добавляет информацию о локализации

Они подключены в `settings.py` по умолчанию:

```python
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]
```

### Создание собственного процессора 🛠️

1. Создаем файл `context_processors.py` в приложении (например, `core/context_processors.py`)
2. Пишем функцию-процессор
3. Регистрируем его в `settings.py`

Пример процессора для меню:

```python
# core/context_processors.py
def menu_processor(request):
    return {
        'menu': [
            {'title': 'Главная', 'url_name': 'home'},
            {'title': 'Услуги', 'url_name': 'services'},
            {'title': 'О нас', 'url_name': 'about'},
            {'title': 'Контакты', 'url_name': 'contacts'},
        ]
    }
```

Регистрация в `settings.py`:

```python
TEMPLATES = [
    {
        'OPTIONS': {
            'context_processors': [
                # ...
                'core.context_processors.menu_processor',
            ],
        },
    },
]
```

### Использование в шаблоне 📝

Теперь меню доступно во всех шаблонах. Обновим `include_nav_menu.html`:

```html
<nav>
    <ul>
        {% for item in menu %}
        <li><a href="{% url item.url_name %}">{{ item.title }}</a></li>
        {% endfor %}
    </ul>
</nav>
```

>[!info]
>
>#### Преимущества контекстных процессоров 🚀
>
>#### Зачем использовать контекстные процессоры?
>
>- **Устранение дублирования**: Убирают дублирование кода в представлениях
>- **Глобальный доступ**: Делают данные доступными во всех шаблонах
>- **Удобство поддержки**: Упрощают изменение общих данных в одном месте
>- **Централизованное управление**: Позволяют управлять общим контекстом централизованно

Теперь мы можем убрать передачу меню из всех представлений, так как данные будут добавляться автоматически через контекстный процессор.
